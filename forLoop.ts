
const users : string[] = ['admin','tester','guest'];

for(let i=0;i<users.length;i++)
{
    //console.log(`Running test for ${users[i]}`);
    console.log(users[i]);
}

//users.forEach((user)=>console.log(user));
users.forEach((user)=>
{
    console.log(user);
});

let nums = [1,2,3];
nums.forEach((num)=>console.log(num));


